const textHolder = document.getElementById('text_holder');
const link_holder = document.getElementById('link_holder');

setTimeout(() => {
    textHolder.classList.remove('hide');
    link_holder.classList.remove('hide')
}, 3500)